# Spec: Support Agent (demo e-commerce store)

Status: **Capability map approved 2026-09-18.** Module specs live in `spec/SPEC-<module-id>.md`.

## Objective

An AI support agent for a small e-commerce store ("Northwind Goods", a fictional home-goods shop).
It answers customer questions from the store's own policies and catalog, looks up order status for
verified customers, and hands off to a human whenever it is not confident or the request is
out of bounds. One agent core serves three channels: web chat, email, WhatsApp.

**Users**
- *Customer* — asks questions on any channel, expects a correct answer or a clear handoff.
- *Support staff* — sees escalated conversations in a queue and takes over.
- *Store owner* — edits policy docs; sees answer/escalation metrics.

## Capability map

| Module id | Responsibility | Depends on |
|---|---|---|
| `knowledge-base` | Ingest FAQ/policies/catalog, chunk, embed, hybrid search | — |
| `orders` | Demo store data + verified order-status lookup | — |
| `handoff` | Escalation tickets, human queue, staff takeover | — |
| `agent-core` | Claude agent: retrieve, call tools, answer-or-escalate; channel-agnostic API | knowledge-base, orders, handoff |
| `evals` | Golden test set; answer correctness + escalation accuracy + data-leak checks in CI | agent-core |
| `channel-webchat` | Embeddable widget + HTTP/SSE endpoint | agent-core |
| `channel-email` | Inbound webhook + outbound reply | agent-core |
| `channel-whatsapp` | WhatsApp Cloud API webhook + send | agent-core |
| `verticals` | Business-specific config, docs, tools and guardrails per industry | agent-core, evals |

Build order: `knowledge-base`, `orders`, `handoff` → `agent-core` → `evals` → `verticals` →
`channel-webchat` → `channel-email` → `channel-whatsapp`

Product direction (2026-09-21): one engine, several industries. `verticals` turns the store-specific
parts into configuration so hospitality, clinics, real estate and coaching run on the same code.
See `spec/SPEC-verticals.md`.

Specs written so far: knowledge-base, orders, handoff, agent-core, evals. Channel specs are written
after `evals` is green, so they build on a measured core.

## Tech stack

- Python 3.11+, FastAPI, Pydantic v2, Uvicorn
- Anthropic Python SDK (Claude). Model set by `ANTHROPIC_MODEL` env var — no hard-coded model ids
- SQLite via SQLAlchemy 2.x (Postgres-compatible schema; swap later without code changes)
- Local embeddings: `fastembed` (BAAI/bge-small-en-v1.5) + BM25 (`rank-bm25`) — no second API key
- pytest, pytest-asyncio, ruff, mypy
- Docker + docker compose
- Dependency management: `uv`

## Commands

```
Install:   uv sync
Dev:       uv run uvicorn app.main:app --reload
Seed:      uv run python -m app.seed            # generates demo store data + ingests docs
Test:      uv run pytest -q
Lint:      uv run ruff check . && uv run ruff format --check .
Types:     uv run mypy app
Evals:     uv run python -m evals.run --min-answer 0.90 --min-escalation 0.95
Docker:    docker compose up --build
```

## Project structure

```
app/
  main.py                 FastAPI app factory, router wiring
  config.py               Settings (pydantic-settings, env vars)
  db.py                   Engine/session
  knowledge_base/         ingest.py, search.py, models.py
  orders/                 models.py, service.py, seed.py
  handoff/                models.py, service.py, routes.py, templates/
  agent/                  core.py, tools.py, prompts.py, policy.py
  channels/               webchat/, email/, whatsapp/   (later)
data/
  docs/                   Store policies/FAQ as markdown (source of truth)
evals/
  golden.jsonl            Test questions with expected outcomes
  run.py                  Eval runner
tests/                    Mirrors app/ layout
spec/                     Module specs
tasks/                    plan.md, todo.md (generated by /plan)
```

## Code style

```python
# Typed, small, pure where possible. Services take a Session; routes stay thin.
def get_order_status(session: Session, order_number: str, email: str) -> OrderStatus | None:
    """Return status only if order_number and email match the same order."""
    order = session.scalar(select(Order).where(Order.number == order_number))
    if order is None or order.customer_email.lower() != email.strip().lower():
        return None  # never reveal whether the order number exists
    return OrderStatus.from_model(order)
```

- snake_case modules/functions, PascalCase classes; ruff defaults, line length 100
- No logic in route handlers beyond validation and calling a service
- All config via `app/config.py`; never read `os.environ` elsewhere
- Log with structured `logging` (JSON formatter); never log message bodies at INFO

## Testing strategy

- Unit tests per service (pytest), co-located under `tests/<module>/`
- Claude calls are behind an interface; unit tests use a fake client. Only `evals/` hits the real API
- Coverage target: 85% lines for `app/` (excluding `channels/` adapters' network code)
- `evals` run in CI on PRs that touch `app/agent/`, `app/knowledge_base/`, `data/docs/`, or `evals/`

## Boundaries

- **Always:** write a failing test first; run lint + tests before commit; one commit per task;
  query codebase-memory-mcp (`get_architecture`, `trace_path`, `detect_changes`) before editing
  code you haven't read this session
- **Ask first:** adding a dependency not listed above; schema changes after `orders` ships;
  changing CI workflows; any real external account (email provider, Meta/WhatsApp, hosting)
- **Never:** commit secrets or `.env`; let the agent issue refunds, cancel orders, or take payments;
  return one customer's order data to another; delete or skip failing tests/evals without approval

## Success criteria (whole project)

- Evals: ≥ 90% answer correctness, ≥ 95% escalation accuracy, **0** cross-customer data leaks
- Web chat p95 time-to-first-token < 2s, full reply < 5s (local, demo data)
- Every PR passes: lint, types, tests, open-code-review with no unresolved blocking comments

## Open questions

- Hosting target for the public demo (Render / Fly.io / Railway) — decide before `channel-webchat` ships
- Email provider (Postmark vs SendGrid inbound parse) — decide before `channel-email`
