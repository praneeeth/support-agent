services:
  app:
    build: .
    ports: ["8000:8000"]
    env_file: [{path: .env, required: false}]
    environment:
      DATABASE_URL: sqlite:////data/support.db
    volumes: [data:/data]
volumes:
  data:
